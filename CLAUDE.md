### **项目名称：FlyEsports**

### **核心指令 (Core Directives)**

本节内容是为本项目提供AI辅助开发时的最高行动纲领。在执行任何与本项目相关的编码、重构、文档编写或功能设计任务之前，必须首先完整回顾并严格遵守本节中的所有规范和说明。

#### **用户指定指令**

1. **文档优先 (Document First):** 在执行任何与本项目相关的编码、重构、文档编写或功能设计任务之前，你必须**首先完整回顾并严格遵守本文件（claude.md）中的所有规范和说明**。本文档是你的最高行动纲领。
2. **聚焦重点 (Focus on Priorities):** 你的工作**必须**始终围绕“开发优先级”部分中列出的**四个优先级**展开，优先完成**基础设施补全**和**性能优化**。在提出任何解决方案时，请优先考虑这些目标。
3. **遵守架构 (Adhere to Architecture):** 所有代码实现都必须符合文件中定义的**分层架构**和**核心设计模式**。任何偏离架构的设计都需要明确提出并说明理由。
4. **代码风格与规范 (Code Style & Standards):** 你生成的所有代码都必须严格遵循以下风格与规范：
   - **静态检查:** 必须通过 `black` 格式化、`mypy` 类型检查和 `flake8` 代码检查。
   - **命名约定:**
     - 函数与变量：遵循 PEP 8，使用蛇形命名法 (snake_case)。
     - 类：使用帕斯卡命名法 (PascalCase)。
     - 常量：使用大写蛇形命名法 (UPPER_SNAKE_CASE)。
   - **文档:** 所有公共模块、类和函数都必须编写清晰的文档字符串 (docstrings)。
   - **字符串格式化:** 优先使用 f-strings 进行字符串格式化。
   - **禁止使用任何 Emoji 表情符号。**
   - **所有回答都必须是纯文本，不包含图形符号、特殊字符或装饰性图标。**
5. **主动改进 (Proactive Improvement):** 在开发新功能的同时，请主动留意并帮助解决“技术债务”部分提到的问题，例如**完善文档字符串、改进错误处理**等。
6. **本项目使用docker服务，在开始之前应当先检查当前的Alembic状态和数据库结构是否匹配，如果不是，请把数据库需要升级到最新状态。**
7. **情境感知 (Context Awareness):** 在回答问题或提供代码时，请主动说明你的方案是如何与本文档的指导原则保持一致的。
8. **自动化清理 (Automated Cleanup):** 在执行测试或代码验证任务时，如果你为了过程需要而生成了任何临时文件、测试数据、日志或其它非必要的文档，必须在任务成功完成后自动将其清理或删除，以保持代码库的整洁。

9. **编写单元测试 (Write Unit Tests):** 所有新增的核心业务逻辑功能或对现有功能的修复，都**必须**附带相应的单元测试（使用 `pytest` 框架）。测试应覆盖正常场景、边界条件和预期的异常情况。

10. **安全第一 (Security First):** 在编写任何处理用户输入或访问数据库的代码时，**必须**主动考虑常见的Web安全漏洞（如SQL注入、跨站脚本XSS、权限绕过等），并采取适当的防御措施（如使用ORM参数化查询、对输出进行转义等）。

11. **规范化提交 (Standardized Commits):** 当生成版本控制相关的操作建议时（如git commit message），**必须**遵循**Conventional Commits**规范。这有助于自动化生成变更日志（Changelog）和清晰地追踪项目历史。例如：`feat: add user registration endpoint` 或 `fix: correct scoring calculation error`。

12. **明确沟通 (Clear Communication):** 当提出一个设计方案或复杂的代码实现时，**必须**以清晰、结构化的方式进行解释，至少包含三个部分：**问题（Problem）**、**解决方案（Solution）\**和\**潜在的权衡/风险（Trade-offs/Risks）**。

13. 在本项目中，沟通语言使用中文进行交流对话。

### **项目背景**

当前，在《英雄联盟》玩家社区中，存在大量由民间爱好者自发组织的线上赛事。这些赛事通常以“赛区”的形式存在，组织者通过建立QQ群等社交工具，将特定区域或社群的玩家聚集起来，引导他们组建战队，并安排队伍之间进行对抗比赛。

然而，随着参与玩家数量的不断增多，这种纯人工的管理模式暴露出了严重的弊端。首先，赛事组织和人员管理变得异常繁琐和混乱，仅依靠社交群组难以进行系统化的信息传达和队伍管理。

其次，也是最核心的痛点在于比赛数据的处理。为了提升赛事的专业性和趣味性，组织者需要统计每场比赛的详细数据（如击杀、死亡、助攻、经济等），并制作成榜单向玩家们展示。目前，这项工作完全依赖人工手动统计，并使用Excel表格进行记录和计算。这不仅效率低下、容易出错，而且数据的展示方式单一，无法给玩家带来良好的互动体验和荣誉感。

因此，FlyEsports平台的创建旨在解决以上问题，用一个系统化、自动化的线上平台来替代目前低效的“QQ群 + Excel”工作流。

------



### **后端技术栈选型**

### **后端技术栈选型**

本项目的后端将采用一套现代化、高性能且支持异步处理的技术栈，以确保平台的响应速度、稳定性和未来的可扩展性。

- **Web框架 (Web Framework): FastAPI**
- **ORM (Object-Relational Mapping): SQLAlchemy 2.0**
- **数据库 (Database): PostgreSQL**
- **缓存 (Caching): Redis**
- **异步任务队列 (Asynchronous Task Queue): Celery**
- **部署与容器化 (Deployment & Containerization): Docker**

### **软件架构设计 (后端)**

为应对FlyEsports复杂的业务场景并保证项目长期的可维护性和扩展性，我们决定采用**领域驱动设计(DDD) + 清洁架构(Clean Architecture)**的混合模式。

#### **核心架构分层**

我们将系统严格划分为四个层次，并遵循“依赖倒置原则”，即所有依赖关系都指向领域层。

```bash
┌─────────────────────────────────────────┐
│              Presentation Layer             │  表现层
│     (API Controllers, WebSocket, CLI)       │
├─────────────────────────────────────────┤
│             Application Layer               │  应用层
│      (Use Cases, Command Handlers)          │
├─────────────────────────────────────────┤
│                 Domain Layer                │  领域层 (核心)
│  (Aggregates, Entities, Value Objects)      │
├─────────────────────────────────────────┤
│            Infrastructure Layer             │  基础设施层
│ (Database, Cache, External APIs, Celery)    │
└─────────────────────────────────────────┘
```

- **1. 领域层 (Domain Layer):** 包含所有独立的业务逻辑、规则和状态。
- **2. 应用层 (Application Layer):** 编排领域对象和基础设施层接口，完成业务用例。
- **3. 基础设施层 (Infrastructure Layer):** 提供所有技术细节的具体实现，如数据库交互、外部API调用等。
- **4. 表现层 (Presentation Layer):** 系统的外部入口，如REST API。

*(注：此处省略了每个分层的详细设计，以保持文档的概览性，完整细节请参考专项设计文档。)*

### **前端技术栈与架构设计**

前端将基于Vue 3生态系统，采用现代化、工程化的方案，以保证开发效率、代码质量和用户体验。

#### **Vue 3现代化技术栈选择**

- **核心框架层: Vue 3 + TypeScript + Vite**
  - **Vue 3:** 提供Composition API，能更好地组织复杂逻辑，且性能更优。
  - **TypeScript:** 为大型项目提供类型安全，减少运行时错误，提升代码可维护性。
  - **Vite:** 新一代前端构建工具，提供极致的开发服务器启动速度和热更新体验。
- **UI组件库: Ant Design Vue 4.x**
  - **选用理由:** 设计语言现代，组件丰富且功能强大，对TypeScript支持良好，非常适合构建数据驱动的管理后台和复杂业务界面。
- **状态管理: Pinia + VueUse**
  - **Pinia:** Vue 3官方推荐的状态管理库，轻量、直观，对TypeScript支持完美，拥有更好的模块化设计。
  - **VueUse:** 一个实用的组合式函数工具库，可以极大简化代码，提升开发效率。
- **路由和导航: Vue Router 4**
  - **特点:** 与Vue 3深度集成，支持动态路由、懒加载，并提供基于Promise的导航守卫，能更好地处理异步路由逻辑。

#### **前端架构设计**

我们将采用**组合式架构 + 特性驱动开发(Feature-Driven Development)**的模式来组织代码。

- **核心思想:** 将代码按业务功能（特性）进行组织，而非按文件类型。这使得项目结构在不断迭代中依然能保持高度的内聚和清晰的边界。

- **推荐目录结构:**

  ```bash
  src/
  ├── features/           # 特性模块（按业务功能划分）
  │   ├── auth/           # 认证模块 (登录、注册)
  │   ├── matches/        # 比赛模块 (比赛详情、战报)
  │   ├── tournaments/    # 赛事模块 (赛事列表、对阵图)
  │   ├── teams/          # 战队模块 (战队创建、成员管理)
  │   ├── players/        # 选手模块 (个人主页、生涯数据)
  │   └── ...             # 其他业务模块
  ├── shared/             # 跨特性共享的资源
  │   ├── components/     # 通用基础组件 (如自定义按钮、布局)
  │   ├── composables/    # 通用组合式API (如usePagination)
  │   ├── utils/          # 通用工具函数 (如日期格式化)
  │   ├── stores/         # 全局共享状态 (如用户信息)
  │   └── types/          # 全局TypeScript类型定义
  ├── assets/             # 静态资源 (图片、字体、样式)
  ├── router/             # 路由配置
  └── main.ts             # 应用入口文件
  ```

### **开发进度记录**

#### **已完成功能模块 (Completed Features)**

##### **优先级3: 赛事创建功能 (Tournament Creation) - 已完成 ✓**

**完成时间:** 2025年9月

**功能概述:** 实现了完整的多步骤赛事创建向导，支持赛事组织者创建和配置电竞赛事。

**技术实现:**
- **主要文件:**
  - `frontend/src/features/tournaments/views/CreateTournamentView.vue` - 主创建页面
  - `frontend/src/features/tournaments/components/creation/TournamentBasicInfo.vue` - 基本信息步骤
  - `frontend/src/features/tournaments/components/creation/TournamentScheduleConfig.vue` - 时间配置步骤
  - `frontend/src/features/tournaments/components/creation/TournamentRulesConfig.vue` - 规则配置步骤
  - `frontend/src/features/tournaments/components/creation/TournamentMediaUpload.vue` - 图片上传步骤
  - `frontend/src/features/tournaments/components/creation/TournamentPreview.vue` - 预览确认步骤

**核心功能特性:**

1. **多步骤创建向导**
   - 5步骤流程：基本信息 → 时间安排 → 规则设置 → 图片上传 → 预览确认
   - 步进导航器与进度条指示
   - 各步骤独立验证逻辑

2. **基本信息配置 (Step 1)**
   - 赛事名称、类型选择 (战队赛/个人赛)
   - 赛区选择 (基于用户权限)
   - 赛事简介编辑
   - 实时预览卡片效果

3. **时间安排管理 (Step 2)**
   - 报名开始/结束时间设置
   - 比赛开始/结束时间配置
   - 智能时间验证和建议
   - 时间线预览展示
   - 快速设置模板

4. **规则配置系统 (Step 3)**
   - 比赛赛制选择 (单败淘汰、双败淘汰、循环积分、瑞士轮)
   - 最大参与数量限制
   - 队伍规模设置 (战队赛)
   - 段位限制配置
   - 规则建议和预览

5. **媒体资源管理 (Step 4)**
   - Logo和横幅图片上传
   - 预设图片库选择
   - 实时预览效果
   - 文件格式和大小验证
   - 图片裁剪和优化

6. **预览确认机制 (Step 5)**
   - 完整赛事信息预览
   - 赛事卡片效果展示
   - 确认检查清单
   - 媒体资源预览
   - 最终创建确认

**用户体验功能:**
- **自动保存草稿**: 防抖机制，每2秒自动保存用户输入
- **离开页面确认**: 浏览器和路由离开前的未保存提醒
- **详细错误处理**: 友好的错误提示和数据验证
- **响应式设计**: 支持桌面端和移动端访问
- **进度可视化**: 清晰的步骤进度和完成状态
- **数据持久化**: 本地存储草稿功能

**技术架构遵循:**
- ✓ Vue 3 Composition API设计模式
- ✓ TypeScript类型安全开发
- ✓ 组件化架构和特性驱动开发
- ✓ Ant Design Vue一致性设计语言
- ✓ 表单验证和数据校验机制
- ✓ 权限系统集成
- ✓ 与现有API接口对接

**代码质量标准:**
- ✓ 遵循项目代码风格规范
- ✓ 完善的组件属性类型定义
- ✓ 清晰的组件间通信机制
- ✓ 适当的错误边界处理
- ✓ 性能优化考虑 (防抖、懒加载)

#### **待开发功能模块 (Pending Features)**

##### **优先级1: 基础设施补全 - 待开发**
- 用户认证和权限管理系统完善
- 数据库模式优化和索引策略
- API接口标准化和文档生成
- 日志记录和监控系统

##### **优先级2: 核心业务功能 - 待开发** 
- 战队管理和成员邀请系统
- 比赛匹配和对战记录功能
- 数据统计和排行榜系统
- 通知和消息推送机制

##### **优先级4: 性能优化与扩展 - 待开发**
- 前端代码分割和懒加载优化
- 数据库查询性能调优
- 缓存策略实现
- CDN和静态资源优化

**备注:** 各功能模块的详细开发计划和技术规范将在具体开发过程中持续更新。