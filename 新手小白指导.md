# 🎓 FlyEsports 新手小白指导

欢迎来到FlyEsports项目！这份指导将帮助完全没有编程经验的小白用户快速上手这个项目。我们会一步步教您如何启动项目、进行开发，以及解决常见问题。

## 📋 目录

- [第一阶段：环境准备](#第一阶段环境准备)
- [第二阶段：上传到GitHub](#第二阶段上传到github)
- [第三阶段：启动项目](#第三阶段启动项目)
- [第四阶段：使用VS Code](#第四阶段使用vs-code)
- [第五阶段：基础开发](#第五阶段基础开发)
- [第六阶段：数据库迁移管理](#第六阶段数据库迁移管理)
- [第七阶段：实战练习](#第七阶段实战练习)
- [常见问题解决](#常见问题解决)
- [学习资源](#学习资源)

---

## 第一阶段：环境准备

### 🛠️ 需要安装的软件

#### 1. Git（版本控制工具）
- **下载地址**: https://git-scm.com/
- **安装建议**: 全部选择默认设置即可
- **验证安装**: 打开命令提示符，输入 `git --version`，看到版本号就是成功了

#### 2. Docker Desktop（最重要！）
- **下载地址**: https://www.docker.com/products/docker-desktop/
- **注意**: 这是最重要的软件，整个项目都依赖它
- **安装后**: 启动Docker Desktop，确保右下角显示Docker图标
- **验证安装**: 命令提示符中输入 `docker --version`

#### 3. VS Code（代码编辑器）
- **下载地址**: https://code.visualstudio.com/
- **推荐扩展**: 
  - Chinese (Simplified) Language Pack（中文界面）
  - Vue Language Features (Volar)
  - Python

#### 4. Node.js（可选，用于前端开发）
- **下载地址**: https://nodejs.org/
- **选择**: LTS（长期支持）版本
- **验证安装**: 命令提示符中输入 `node --version`

### ✅ 安装验证清单

在命令提示符中依次运行以下命令，确认都能显示版本号：
```bash
git --version
docker --version  
code --version
node --version
```

---

## 第二阶段：上传到GitHub

### 🌐 创建GitHub账户

1. 访问 https://github.com/
2. 点击 "Sign up" 注册账户
3. 验证邮箱地址

### 📁 创建新仓库

1. 登录GitHub后，点击右上角 "+" → "New repository"
2. 仓库设置：
   - **Repository name**: `FlyEsports`
   - **Description**: `电竞赛事管理平台`
   - **Visibility**: 选择 Private（私有）或 Public（公开）
   - **⚠️ 重要**: 不要勾选任何初始化选项（README, .gitignore等）
3. 点击 "Create repository"

### 🔗 连接本地项目到GitHub

1. 复制GitHub上显示的仓库地址（格式：https://github.com/你的用户名/FlyEsports.git）
2. 打开命令提示符，进入项目目录：
   ```bash
   cd "E:\Project\GitHub Repo\FlyEsports"
   ```
3. 运行以下命令（替换为您的仓库地址）：
   ```bash
   git remote add origin https://github.com/mapleccs/FlyEsports.git
   git push -u origin main --tags
   git push -u origin develop
   ```

如果遇到认证问题，GitHub会提示您设置个人访问令牌，按提示操作即可。

---

## 第三阶段：启动项目

### 🚀 一键启动（推荐新手）

这是最简单的方式，Docker会自动处理所有复杂的配置：

1. **打开命令提示符**，进入项目目录：
   ```bash
   cd "E:\Project\GitHub Repo\FlyEsports"
   ```

2. **启动所有服务**：
   ```bash
   docker-compose up -d
   ```
   
   > ⏰ 首次启动需要下载镜像，大约需要5-10分钟，请耐心等待！

3. **检查服务状态**：
   ```bash
   docker-compose ps
   ```
   看到所有服务都显示 "Up" 状态就成功了。

### 🌐 访问应用

启动成功后，可以访问以下地址：

| 服务 | 地址 | 说明 |
|------|------|------|
| 前端网站 | http://localhost:3000 | 主要的用户界面 |
| 后端API | http://localhost:8000 | 服务器接口 |
| API文档 | http://localhost:8000/docs | 接口测试页面 |
| 数据库管理 | http://localhost:5555 | 监控面板 |

### 🔧 常用Docker命令

```bash
# 启动所有服务
docker-compose up -d

# 查看服务状态
docker-compose ps

# 查看服务日志（出现问题时使用）
docker-compose logs

# 重启服务
docker-compose restart

# 停止所有服务
docker-compose down
```

---

## 第四阶段：使用VS Code

### 📂 打开项目

1. 启动VS Code
2. 点击 "文件" → "打开文件夹"
3. 选择项目目录：`E:\Project\GitHub Repo\FlyEsports`
4. 点击 "选择文件夹"

### 📁 了解项目结构

```
FlyEsports/
├── frontend/              # 前端代码（用户界面）
│   ├── src/
│   │   ├── views/         # 页面文件
│   │   ├── features/      # 功能模块
│   │   └── components/    # 组件文件
├── backend/               # 后端代码（服务器逻辑）
│   ├── src/
│   │   ├── presentation/  # API接口
│   │   ├── domain/        # 业务逻辑
│   │   └── infrastructure/# 基础设施
├── docker-compose.yml     # Docker配置
└── README.md             # 项目说明
```

### 🎯 新手重点关注的文件

- `frontend/src/views/Home.vue` - 首页
- `frontend/src/views/` - 其他页面
- `backend/src/presentation/api/routes/` - API接口
- `docker-compose.yml` - 服务配置

---

## 第五阶段：基础开发

### 📝 Git基本操作

开发过程中，您需要掌握这几个Git命令：

```bash
# 1. 查看文件修改状态
git status

# 2. 添加修改的文件
git add .

# 3. 提交修改（按规范格式）
git commit -m "feat: 添加了新功能"

# 4. 推送到GitHub
git push
```

### 📋 提交信息规范（简化版）

提交信息格式：`类型: 描述`

常用类型：
- `feat:` 新功能
- `fix:` 修复问题  
- `docs:` 文档更新
- `style:` 样式调整

示例：
```bash
git commit -m "feat: 添加用户登录页面"
git commit -m "fix: 修复登录按钮点击问题"
git commit -m "style: 优化首页布局"
```

### 🔄 开发流程

1. **修改代码** → 2. **保存文件** → 3. **查看效果** → 4. **提交更改** → 5. **推送到GitHub**

---

## 第六阶段：数据库迁移管理

### 🎯 什么是数据库迁移？

数据库迁移是管理数据库结构变更的方式。当您需要：
- 添加新表或字段
- 修改现有字段类型
- 删除不需要的字段
- 创建索引或约束

都需要通过数据库迁移来安全地实现这些变更。

### 🛠️ 小白友好的迁移工具

我们为您准备了一个超级简单的迁移管理脚本，让数据库操作变得像聊天一样简单！

#### 基础命令速查表

```bash
# 1. 查看数据库状态（最常用）
python backend/scripts/migrate.py status

# 2. 创建新迁移（当您修改了模型文件后）
python backend/scripts/migrate.py create "您的修改描述"

# 3. 应用迁移到数据库
python backend/scripts/migrate.py upgrade

# 4. 查看迁移历史
python backend/scripts/migrate.py history

# 5. 回滚迁移（小心使用）
python backend/scripts/migrate.py downgrade
```

#### 🔄 Docker环境下的使用方法

我们为您准备了**超级简单**的一键式脚本！

**Windows用户（推荐）:**
```bash
# 直接双击运行或在命令行中使用
migrate.bat status
migrate.bat create "添加用户头像字段"  
migrate.bat upgrade
migrate.bat history
```

**Linux/Mac用户:**
```bash
# 给脚本执行权限（只需要第一次）
chmod +x migrate.sh

# 使用脚本
./migrate.sh status
./migrate.sh create "添加用户头像字段"
./migrate.sh upgrade  
./migrate.sh history
```

**如果您喜欢传统方式:**
```bash
# 方式1：在后端容器中执行
docker-compose exec backend python scripts/migrate.py status

# 方式2：直接运行Python脚本
python backend/scripts/migrate.py status
```

### 📋 完整操作流程（新手版）

#### 场景1：我要添加一个新字段

**步骤1：修改模型文件**
1. 打开您需要修改的模型文件（在`backend/src/infrastructure/database/models/`目录下）
2. 添加新字段，例如：
   ```python
   avatar_url: Mapped[Optional[str]] = mapped_column(String(255), nullable=True)
   ```

**步骤2：创建迁移文件**
```bash
# 在项目根目录执行
python backend/scripts/migrate.py create "添加用户头像字段"
```

**步骤3：检查生成的迁移文件**
- 查看`backend/alembic/versions/`目录下新生成的文件
- 确认迁移内容符合预期

**步骤4：应用迁移**
```bash
python backend/scripts/migrate.py upgrade
```

**步骤5：验证结果**
```bash
python backend/scripts/migrate.py status
```

#### 场景2：我想查看当前数据库状态

```bash
# 这个命令会告诉您：
# - 当前数据库版本
# - 是否需要更新
# - 迁移文件总数
python backend/scripts/migrate.py status
```

输出示例：
```
🚀 数据库迁移状态
====================================
📊 当前版本: abc123def456
📊 最新版本: abc123def456  
📊 迁移文件总数: 5
✅ 数据库已是最新版本
```

#### 场景3：出错了怎么办？回滚迁移

**⚠️ 重要：回滚会丢失数据，请谨慎操作！**

```bash
# 回滚最后一个迁移
python backend/scripts/migrate.py downgrade

# 回滚多个迁移（例如回滚3步）
python backend/scripts/migrate.py downgrade --steps 3
```

### 🚨 安全提醒

#### ✅ 安全操作
- 在开发环境随意测试
- 修改前先用`status`查看状态
- 每次只修改一个功能
- 先在本地测试成功再推送

#### ❌ 危险操作
- 不要在生产环境随意回滚
- 不要手动修改迁移文件（除非您很懂）
- 不要跳过版本直接修改数据库
- 不要在多人协作时随意重置数据库

### 💡 实用技巧

#### 1. 查看详细的迁移历史
```bash
python backend/scripts/migrate.py history --verbose
```

#### 2. 迁移失败了怎么办？
```bash
# 先查看状态
python backend/scripts/migrate.py status

# 查看Docker日志找到具体错误
docker-compose logs backend

# 如果数据库连接失败，重启服务
docker-compose restart postgres backend
```

#### 3. 团队协作注意事项
```bash
# 每天开始工作前，先拉取最新代码
git pull

# 检查是否有新的迁移需要应用
python backend/scripts/migrate.py status

# 如果有待处理的迁移，先应用
python backend/scripts/migrate.py upgrade
```

### 🆘 常见问题与解决方案

#### Q: 提示"数据库连接失败"
```bash
解决步骤：
1. 检查Docker服务：docker-compose ps
2. 重启数据库：docker-compose restart postgres
3. 等待30秒后重试
4. 检查端口是否被占用
```

#### Q: 创建迁移时提示"没有检测到变更"
```bash
解决步骤：
1. 确认您真的修改了模型文件
2. 保存文件（Ctrl+S）
3. 检查语法是否正确
4. 重启后端服务：docker-compose restart backend
```

#### Q: 迁移执行失败
```bash
解决步骤：
1. 查看具体错误信息
2. 检查数据库中是否有冲突数据
3. 如果是开发环境，可以考虑重置：
   python backend/scripts/migrate.py reset
4. 如果是生产环境，请联系技术人员
```

#### Q: 我想彻底重置数据库
```bash
⚠️ 警告：这将删除所有数据！仅在开发环境使用！

python backend/scripts/migrate.py reset
```

### 📝 迁移命令参考表

| 命令 | 用途 | 危险等级 | 使用频率 |
|------|------|----------|----------|
| `status` | 查看状态 | 🟢 安全 | 每天 |
| `create` | 创建迁移 | 🟡 注意 | 开发时 |
| `upgrade` | 应用迁移 | 🟡 注意 | 开发时 |
| `history` | 查看历史 | 🟢 安全 | 偶尔 |
| `downgrade` | 回滚迁移 | 🔴 危险 | 很少 |
| `reset` | 重置数据库 | ⚠️ 极危险 | 极少 |

---

## 第七阶段：实战练习

### 🎯 练习1：修改首页文字

让我们从最简单的开始：

1. **找到首页文件**：
   - 在VS Code中打开 `frontend/src/views/Home.vue`

2. **修改内容**：
   - 找到 `<h1>` 标签，修改其中的文字
   - 例如：将 "欢迎来到FlyEsports" 改为 "欢迎来到我的电竞平台"

3. **保存并查看效果**：
   - 按 `Ctrl+S` 保存文件
   - 打开浏览器访问 http://localhost:3000
   - 应该能看到文字变化

4. **提交更改**：
   ```bash
   git add .
   git commit -m "style: 修改首页标题文字"
   git push
   ```

### 🎯 练习2：添加新页面

1. **复制现有页面**：
   - 复制 `frontend/src/views/Home.vue`
   - 重命名为 `About.vue`

2. **修改内容**：
   - 将页面内容改为关于页面的内容

3. **添加路由**：
   - 打开 `frontend/src/router/index.ts`
   - 仿照现有路由添加新的路由配置

4. **测试访问**：
   - 访问新页面地址，确认能正常显示

### 🎯 练习3：修改API接口

1. **找到接口文件**：
   - 打开 `backend/src/presentation/api/routes/auth.py`

2. **修改响应内容**：
   - 找到某个接口的返回内容，修改其中的文字

3. **测试接口**：
   - 访问 http://localhost:8000/docs
   - 找到对应的接口进行测试

---

## 常见问题解决

### ❓ Docker问题

**Q: Docker启动很慢或失败**
```bash
# 解决方案：
1. 确保Docker Desktop正在运行
2. 重启Docker Desktop
3. 清理Docker缓存：docker system prune
4. 检查端口是否被占用
```

**Q: 端口占用错误**
```bash
# 解决方案：
1. 修改 docker-compose.yml 中的端口号
2. 或者停止占用端口的其他程序
```

**Q: 服务无法访问**
```bash
# 检查步骤：
1. 运行 docker-compose ps 查看服务状态
2. 运行 docker-compose logs 查看错误日志
3. 重启服务：docker-compose restart
```

### ❓ 代码问题

**Q: 修改代码后没有变化**
```
解决方案：
1. 确认文件已保存（Ctrl+S）
2. 刷新浏览器页面（F5）
3. 检查浏览器控制台是否有错误信息
4. 重启Docker服务
```

**Q: 页面显示错误**
```
解决方案：
1. 按F12打开浏览器开发者工具
2. 查看Console标签页的错误信息
3. 根据错误信息修复代码
4. 查看Network标签页检查API请求
```

### ❓ Git问题

**Q: 推送失败**
```bash
# 解决方案：
1. 检查网络连接
2. 确认GitHub仓库地址正确
3. 设置GitHub访问令牌
4. 先拉取最新代码：git pull
```

**Q: 提交冲突**
```bash
# 解决方案：
1. 先拉取远程代码：git pull
2. 解决冲突后重新提交
3. 或者重置到之前状态：git reset --hard HEAD~1
```

---

## 学习资源

### 📚 项目内文档（必读）

1. **README.md** - 项目概述和快速开始
2. **CONTRIBUTING.md** - 详细的开发规范
3. **新手小白指导.md** - 本文档，新手必读
4. **backend/docs/DATABASE_MIGRATION_GUIDE.md** - 数据库迁移详细指南
5. **frontend/src/** - 查看现有的Vue组件示例
6. **backend/src/presentation/api/routes/** - 查看API示例

### 🛠️ 项目专用工具

我们为您准备了专门的小白友好工具：

**数据库迁移工具:**
- `migrate.bat` (Windows) / `migrate.sh` (Linux/Mac) - 一键式迁移脚本
- `backend/scripts/migrate.py` - Python迁移管理脚本
- `backend/docs/DATABASE_MIGRATION_GUIDE.md` - 详细使用指南

**使用方法:**
```bash
# Windows用户
migrate.bat help     # 查看所有命令
migrate.bat status   # 最常用：查看数据库状态

# Linux/Mac用户  
./migrate.sh help
./migrate.sh status
```

### 🌐 在线教程（推荐）

**前端开发：**
- [Vue 3 官方教程](https://cn.vuejs.org/tutorial/) - 最权威的Vue学习资源
- [TypeScript 入门](https://typescript.bootcss.com/) - 类型系统学习

**后端开发：**
- [FastAPI 教程](https://fastapi.tiangolo.com/zh/) - 现代Python Web框架
- [Python 基础](https://docs.python.org/zh-cn/3/tutorial/) - Python官方教程

**工具使用：**
- [Git 教程](https://www.liaoxuefeng.com/wiki/896043488029600) - 版本控制基础
- [Docker 入门](https://docs.docker.com/get-started/) - 容器化技术

### 🎯 学习路径建议

**第1周：环境熟悉**
- 能够启动项目
- 学会基本的Git操作
- 熟悉VS Code使用

**第2周：前端入门**
- 学习Vue 3基础语法
- 修改现有页面
- 添加简单的新页面

**第3周：后端入门**  
- 了解FastAPI基础
- 修改现有API接口
- 添加简单的新接口

**第4周：综合实践**
- 完成一个完整的功能
- 前后端联调
- 掌握调试技巧

### ⚠️ 新手避坑指南

1. **不要同时修改太多文件** - 一次只改一个功能
2. **保存文件很重要** - Ctrl+S 是你的好朋友
3. **遇到错误先看日志** - 错误信息通常很有用
4. **不懂就看示例** - 项目里有很多可参考的代码
5. **提交前先测试** - 确保功能正常再提交代码

### 🆘 获取帮助的方式

1. **查看错误信息** - 90%的问题都能从错误信息找到线索
2. **搜索项目文档** - 使用Ctrl+F在文档中搜索关键词
3. **Google搜索** - 搜索具体的错误信息或问题
4. **GitHub Issues** - 在项目仓库中提出问题
5. **询问AI助手** - 可以询问具体的代码问题

---

## 🎉 结语

恭喜您！现在您已经具备了使用FlyEsports项目进行开发的基础知识。记住：

- **编程是一个循序渐进的过程**，不要着急
- **遇到问题是正常的**，关键是学会如何解决
- **多实践、多尝试**，熟能生巧
- **不懂就查、就问**，没有人天生就会编程

祝您开发愉快！🚀

---

*如果这份指导对您有帮助，请给项目点个星⭐*